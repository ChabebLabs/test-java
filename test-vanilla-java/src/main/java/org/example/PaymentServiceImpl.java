package org.example;

// PaymentServiceImpl.java
public class PaymentServiceImpl implements PaymentService {
    @Override
    public boolean processPayment(double amount) {
        // Implémentation réelle pour effectuer le paiement
        return true; // Suppose que le paiement est toujours réussi pour la simplicité
    }
}
